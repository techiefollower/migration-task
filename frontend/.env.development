# Backend HTTP URL for Vite proxy. Must match RepoMigration.Api (launchSettings → applicationUrl, usually http port 5096).
# Use 127.0.0.1 if localhost resolves oddly. IIS Express: use its HTTP URL from the VS debug toolbar.
VITE_API_PROXY_TARGET=http://127.0.0.1:5096
